name: webhook
on:
  workflow_dispatch:
    

jobs:
  JSONvalue:
    name: Print event
    runs-on: ubuntu-latest
    outputs:
     resulting: ${{ steps.set-value.outputs.result }}
    steps:
      - name: checking the JSON value
        id: set-value
        run: | 
          echo "result='perl -pe 's/\s+//g' NSI.json`" >> $GITHUB_OUTPUT
          echo "$resulting"

    
   
        
